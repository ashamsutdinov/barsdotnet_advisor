@using System.Collections.Generic;
@using UI.Models;
@model IEnumerable<commentmodel>
    @{
        ViewBag.Title = "Отзывы";
    }
    <br>
    <table>
        <tr>

            <td width="20%"></td>
            <td width="60%"> <table>
                <h2><font color="green"><center> Отзывы на товар или продукт:</center></font></h2>
                <br><br>
                @if (Model.Count() == 0)
                {
                    <p>К сожалению, еще никто не оставил отзывов на это предложение</p>
                }
                else
                {
                    foreach (CommentModel b in Model)
                    {
                <tr><hr width="100%" align="left">
                    <font color="green">
                        <td align="left">
                            <a href="/UserData/Index/@b.Id">@b.AuthorLogin</a>/td>
                        <td align="right">@b.DateOfCreate</td>
                    </font>
                </tr>
                    <tr>
                    <td>@b.Text</td>
                </tr>
                    }
                }
                </table>
                </td>
                <td width="20%"></td>

                
                </tr>
            </table>
