@using System.Collections.Generic;
@using UI.Models;

@model IEnumerable<CommentModel>
    @{
        ViewBag.Title = "Отзывы";
    }
    <h2>Отзывы на товар или продукт:</h2>
    @if (Model.Count()==0)
    {
        <p>К сожалению, еще никто не оставил отзывов на это предложение</p>
    }
    else
    { 
    foreach (CommentModel b in Model)
    {
    <tr>
        <td><p><a href="/UserData/Index/?login=@b.AuthorLogin">@b.AuthorLogin</a></p></td>
        <td><p>@b.DateOfCreate</p></td>
        <td><p>@b.Text</p></td>
        <td><p></p></td>
    </tr>
    }
    }